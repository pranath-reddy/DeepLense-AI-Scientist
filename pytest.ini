[pytest]
# Collect only the test suite (the example scripts under scripts/ are not tests and
# connect to live models at import time).
testpaths = tests
filterwarnings =
    ignore:There is no current event loop:DeprecationWarning
